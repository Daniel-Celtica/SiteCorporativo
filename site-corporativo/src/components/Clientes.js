const Clientes = () => (
    <div>
        <p className="text-xl font-medium">Image Section</p>
        <img src={subsection.Image} alt={subsection.Title} className="mt-2 w-full h-auto" />
    </div>
);

export default Clientes;